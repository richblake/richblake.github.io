title: "Rich Blake"
author: "Rich Blake"

url: "https://richblake.github.io"
baseurl: ""

repository: richblake/richblake.github.io

collections:
  pages:
    output: true
    permalink: /:name
  posts:
    output: true
    permalink: /blog/:slug

defaults:
  -
    scope:
      path: ""
    values:
      layout: "default"
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

markdown: kramdown

kramdown:
  input: GFM
  syntax_highlighter: rouge

sass:
  style: compressed

plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-redirect-from

paginate: 6
paginate_path: "/blog/page:num/"

# Keep build/docs files out of the published site
exclude:
  - scripts/
  - vendor/
  - Gemfile
  - Gemfile.lock
  - CHANGES.md
  - RUNNING-LOCALLY.md
  - .editorconfig
